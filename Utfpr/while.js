function contar(n) {
    let i = 1
    while (i <= 10) { 
        console.log(`${n} x ${i} = ${n * i}`)
        i++
    }
}
contar(5)

function calcular(n) {
    for (let i= 0; i<=10; i++) {
        console.log(`${n} x ${i} = ${n * i}`)
    }
}

calcular(7)